def get_now_snapshot():
    
def get_planned_windows(lookahead_days: int = 3):
    
def compute_commute_exposure(plan_df, am_window=(7,10), pm_window=(16,19)):
    
def compute_route_reliability(now_df, exposure_df, weights=(w1, w2, w3)):
    